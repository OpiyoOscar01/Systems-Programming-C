/**
 * C libraries provide a collection of functions and macros that can be used to perform various tasks. Some of the most commonly used C standard libraries:

<stdio.h>: Standard Input and Output library

Functions: printf(), scanf(), fprintf(), fscanf(), fgets(), fputs(), fopen(), fclose(), etc.
Usage: Handling input and output operations, file handling.
<stdlib.h>: Standard Library

Functions: malloc(), calloc(), realloc(), free(), atoi(), atof(), rand(), srand(), exit(), etc.
Usage: Memory allocation, process control, conversions, random number generation.
<string.h>: String Handling library

Functions: strlen(), strcpy(), strncpy(), strcat(), strncat(), strcmp(), strncmp(), strchr(), strstr(), etc.
Usage: String manipulation and handling.
<math.h>: Mathematics library

Functions: sin(), cos(), tan(), sqrt(), pow(), exp(), log(), ceil(), floor(), etc.
Usage: Performing mathematical operations.
<time.h>: Time and Date library

Functions: time(), difftime(), clock(), asctime(), ctime(), gmtime(), localtime(), strftime(), etc.
Usage: Handling date and time operations.
<ctype.h>: Character Handling library

Functions: isalpha(), isdigit(), isalnum(), isupper(), islower(), toupper(), tolower(), etc.
Usage: Character classification and conversion.
<assert.h>: Diagnostics library

Functions: assert()
Usage: Providing a way to perform runtime assertions.
<errno.h>: Error Handling library

Macros: errno, EDOM, ERANGE, etc.
Usage: Error reporting.
<limits.h>: Defines the sizes of basic types

Macros: CHAR_BIT, CHAR_MAX, INT_MAX, LONG_MAX, etc.
Usage: Defining implementation-specific limits of data types.
<float.h>: Defines the limits of floating-point types

Macros: FLT_MAX, DBL_MAX, LDBL_MAX, etc.
Usage: Defining implementation-specific limits of floating-point types.
<stddef.h>: Standard definitions

Macros and types: NULL, size_t, ptrdiff_t, offsetof(), etc.
Usage: Common definitions and types.
<signal.h>: Signal Handling library

Functions: signal(), raise(), sigaction(), etc.
Usage: Handling signals.
 * 
 * 
 */